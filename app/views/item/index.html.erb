<div><p>Welcome to the item management system. <a href="/item/new">Click here to add a new item.</a></p></div>

<div><p>Please click on an item's name to show details and notes about the item!</p></div>

<table class="pretty">
  <tr>
    <th><%= sortable "name" %></th>
    <th><%= sortable "barcode" %></th>
	<th><%= sortable "category_id", "Category" %></th>
	<th><%= sortable "type_id", "Type" %></th>
    <th><%= sortable "status" %></th>
	<th><%= sortable "condition" %></th>
	<th><%= sortable "borrow_count", "Times borrowed" %></th>
	<th><%= sortable "borrower_id" %></th>
	<th>Edit item?</th>
	<th>Delete item?</th>
  </tr>
  <% for item in @items %>
    <tr>
      <td><%= link_to item.name, "/item/#{item.id}" %></td>
      <td><%= item.barcode %></td>
	  <td><%= item.category.name %></td>
	  <td><%= item.type.name %></td>
	  <td><%= item.status %></td>
      <td><%= item.condition %></td>
	  <td><%= item.borrow_count %></td>
	  <td>
	  <% if item.borrower_id.nil? %>
	  none
	  <% else %>
	  <%= item.user.fullname %></td>
	  <% end %>
	  <td><%= link_to "x", "/item/#{item.id}/edit" %></td>
	  <td><%= link_to "x", "/item/#{item.id}", :method => "delete" %></td>
    </tr>
  <% end %>
</table>